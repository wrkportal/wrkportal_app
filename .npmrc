# Use npm ci for faster, reliable, reproducible builds
# This ensures package-lock.json is used and node_modules is clean
# Allow legacy peer deps to resolve conflicts (next-auth beta requires Next 14/15, we use Next 16)
legacy-peer-deps=true
prefer-offline=true
progress=false
