-- Baseline migration: Sync all tables created by `prisma db push` on 2026-04-06
-- This migration was applied via `prisma db push` and then marked as applied
-- to bring the migration history in sync with the actual database state.
--
-- All tables, enums, indexes, and foreign keys from the Prisma schema that were
-- missing from previous migrations are now present in the database.
-- This is a no-op migration that serves as a checkpoint.

-- No SQL needed - db push already created all missing objects.
-- This file exists to keep prisma migrate deploy in sync.
SELECT 1;
