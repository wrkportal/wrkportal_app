# Install PDFKit for Evidence Pack PDF Generation

## 📦 Quick Installation

Run this command in your project root:

```bash
npm install pdfkit
```

Then restart your development server:

```bash
# Stop the server (Ctrl+C)
# Then start again
npm run dev
```

---

## ✅ That's It!

After installation, the Evidence Pack button will:
- ✅ Download PDF directly (like CSV export)
- ✅ No sidebar capture issues
- ✅ Professional formatting
- ✅ Ready to use!

---

## 🧪 Test It

```
1. Go to Admin → Audit Log
2. Click "Evidence Pack" button
3. PDF should download automatically
4. Open the PDF and verify content
```

---

## 🔧 If It Doesn't Work

### **Check Installation:**
```bash
npm list pdfkit
```

Should show: `pdfkit@x.x.x`

### **Reinstall if Needed:**
```bash
npm uninstall pdfkit
npm install pdfkit
```

### **Check Server Logs:**
Look for errors in the terminal where `npm run dev` is running.

---

## 📋 What Gets Installed

- **pdfkit** - PDF generation library
- **Dependencies** - Automatically installed by npm

**Total size:** ~2-3 MB

---

## 🎉 Done!

Once installed, both export buttons work identically:
- **Export** → CSV download
- **Evidence Pack** → PDF download

Simple and professional! ✨

